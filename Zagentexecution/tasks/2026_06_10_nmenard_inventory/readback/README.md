# Readback — session artifact only

The CANONICAL copies of these sources live in the golden ABAP corpus (brain-ingested):
`extracted_code/HCM/YHR_PA_WF/`

The object catalog from this scan is in the Gold DB (`p01_gold_master_data.db`):
`d01_tadir_yhr_pa_wf` · `d01_tadir_nmenard` · `d01_tfdir_nmenard_fugr`.

Edit/extend the golden copies, not these.
