{
 "_concept": "ALGORITHMS — a first-class asset class, not content hidden inside scripts",
 "_one_line": "The value of an analysis product IS its algorithms. Everything else — the data, the findings, the documents — is either an input to them or an output of them.",
 "_version": "1.0",
 "_created_session": 97,
 "_why_first_class": {
  "the_audit": "The asset registry declared five kinds — gold_table, store, tool, doc, gold_database — all of them ARTIFACTS. Zero algorithms. The techniques existed only as implicit content inside scripts, which made them invisible: we could not say which algorithm runs where, reuse one deliberately, compare ours against the market's, or improve one and know what it affected.",
  "the_market_argument": "What a buyer pays Celonis or Signavio for is not their storage and not their dashboards — it is conformance checking, variant analysis, object-centric mining. The algorithms ARE the product. Ours being undeclared meant the product was undeclared.",
  "the_sharper_argument": "An algorithm fails differently from a tool. A broken tool fails loudly. A subtly wrong algorithm produces confident, plausible, WRONG output. This session produced four: a greedy classifier rule that stole FTBB from bank statements; a substring matcher that linked 337 claims to 'CO'; a wrong denominator that made a 35% gap of which half was a category error; a swallowed generic error that masked an invalid field for three runs. You cannot review what you have not named.",
  "the_portability_argument": "A tool is one binding of an algorithm to one problem, one schema, one tenant. The ALGORITHM is what travels to installation #2. Data ages, findings age, tools are bindings — algorithms compound."
 },
 "_fields": {
  "operates_on": "the data class it consumes — logs / process events / repository / data / model / interfaces",
  "origin": "MARKET_STANDARD (adopted) · MARKET_EXTENDED (adopted and improved) · OURS (no market equivalent)",
  "state": "STRONG · WORKS · WEAK · FRAGILE · MISSING",
  "bound_in": "the tool(s) that implement it — an algorithm with no binding is a paper",
  "failure_mode": "how this algorithm produces a WRONG answer without failing loudly",
  "improve": "the specific next lever"
 },
 "algorithms": {
  "A1_chunked_temporal_read": {
   "operates_on": "logs",
   "origin": "OURS",
   "state": "STRONG",
   "does": "read a purging log in bounded time windows (<=6h) because a wide call hangs and the connection degrades every ~12 calls",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/accumulate_logs.py"
   ],
   "failure_mode": "a silently truncated window looks like a quiet period in the data",
   "improve": "derive the chunk size from observed response time instead of a constant"
  },
  "A2_rolling_window_accumulation": {
   "operates_on": "logs",
   "origin": "OURS",
   "state": "FRAGILE",
   "does": "capture a window the source purges (7-120 days) into *_history tables, turning a window into a history",
   "market_equivalent": "NONE — commercial tools connect live and inherit the system's retention",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/accumulate_logs.py"
   ],
   "why_fragile": "gitignored, unversioned, writing to a local database with no confirmed backup — and it cannot be back-filled at any price",
   "failure_mode": "a missed day is invisible: the history simply has a hole nobody sees",
   "improve": "make 'days of history captured' a first-class metric; version the script"
  },
  "A3_two_axis_classification": {
   "operates_on": "logs",
   "origin": "MARKET_EXTENDED",
   "state": "STRONG",
   "does": "explain every call on two independent axes — PROCESS (from the object name) and ORIGIN (from host/destination/user)",
   "why_extended": "process mining assigns activities but does not systematically resolve WHO CALLED. Without origin, a middleware call and an internal report are indistinguishable.",
   "produced": "the 80.6%-external finding; 91.1% of business calls explained",
   "bound_in": [
    "process_mining/rfc_process_classifier.py"
   ],
   "failure_mode": "an unresolved origin collapses a fleet into one label — 174 middleware endpoints currently read as one caller",
   "improve": "resolve endpoint GUIDs from the middleware side; SAP cannot name them"
  },
  "A4_ordered_classifier_ladder": {
   "operates_on": "logs",
   "origin": "OURS",
   "state": "WORKS",
   "does": "ordered rule chain (package -> software component -> overlay -> name -> text -> substrate); first match wins, specificity expressed by ORDER",
   "bound_in": [
    "process_mining/executed_objects_domain_map.py"
   ],
   "failure_mode": "ORDER-DEPENDENCE IS SILENT. A greedy early rule steals from a later one and nothing errors. Proven twice in s097: 'FTB' swallowed FTBB (bank statements) into deal management; an inline comment swallowed two dict entries and moved 70,766 executions between domains.",
   "improve": "replace the package-regex rung with the DF14L component lookup, which is authoritative. Regex becomes the fallback, not the primary. THIS IS PRIORITY 2 OF THE WHOLE MODEL."
  },
  "A5_adaptive_learning_loop": {
   "operates_on": "logs",
   "origin": "OURS",
   "state": "WORKS",
   "does": "auto-resolve unknown calls by function group / naming / app domain, LEARN the resolution, re-classify until convergence, expose the remainder as an explicit frontier",
   "why_it_matters": "the closest thing the model has to learning — each run explains more with zero hand-coding",
   "bound_in": [
    "process_mining/adaptive_discovery.py"
   ],
   "failure_mode": "a rule learned WRONG persists forever and invisibly — DEMONSTRATED on its first run with the new signal: mapping the broad technical bucket Basis_Security to one business process taught it that IDOCS_OUTPUT_TO_R3, IDOC_INBOUND_ASYNCHRONOUS and BAPI_EXCHANGERATE_GETDETAIL are identity management. 7 rules purged; the mapping removed so it cannot be relearned. Returning NOTHING is correct for a technical call: it goes to the frontier for human review, which is where it belongs. Precision beats coverage when the engine remembers its mistakes.",
   "improve": "give it the reads/writes signal (C3) so it can resolve custom FMs that have no curated overlay entry",
   "fed_by": "C1/C1b/C1c component resolution — the authoritative rung, s097",
   "result_s097": "8 -> 25 learned rules, +17 ALL from the authoritative signal, zero hand-coding. Coverage 92.7%. Frontier 224 FMs / 225,568 calls (7.3%), mostly kernel FMs not in TFDIR at all."
  },
  "A6_frontier_with_substrate_tier": {
   "operates_on": "logs",
   "origin": "OURS",
   "state": "STRONG",
   "does": "coverage % + explicit worklist, with a third tier for technical substrate (connectivity, session, monitoring): real execution, legitimately non-business",
   "produced": "unexplained execution fell from 40% to 11.7% without hiding anything",
   "bound_in": [
    "process_mining/executed_objects_domain_map.py"
   ],
   "failure_mode": "substrate becomes a dumping ground if it is ever matched before business rules — it is matched LAST on purpose",
   "improve": "watch the TREND, not the size: a frontier that stops moving means the discovery loop stopped running"
  },
  "B1_dfg_discovery": {
   "operates_on": "process events",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "build the process map from an event log — activities as nodes, observed transitions as edges",
   "market_equivalent": "pm4py — the reference implementation (van der Aalst lineage), the substrate Celonis and Signavio productised",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/sap_process_discovery.py"
   ],
   "failure_mode": "a DFG on the wrong case notion produces a plausible map of a process that does not exist",
   "improve": "two event sources are wired (transactions AND change documents); the change-document source is richer for lifecycle and under-used"
  },
  "B2_variant_analysis": {
   "operates_on": "process events",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "group cases by exact path, rank by frequency and cost — the long tail is where exceptions live",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/sap_process_discovery.py"
   ],
   "failure_mode": "variants computed on a truncated log look like process simplicity"
  },
  "B3_performance_bottleneck": {
   "operates_on": "process events",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "cycle time per transition; waiting versus processing",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/sap_process_discovery.py"
   ],
   "failure_mode": "timestamps from different systems make a bottleneck out of a clock skew"
  },
  "B4_declarative_conformance": {
   "operates_on": "process events",
   "origin": "MARKET_STANDARD",
   "state": "STRONG",
   "does": "state the normative rules, classify every case against them, quantify the deviation in money",
   "why_it_matters": "THIS IS WHERE THE PRODUCT LIVES — the delta between AS-DESIGNED and AS-RUN",
   "proven": "P2P: 3-way match, GR strictly before IR — 38% conformant, 70 IR-before-GR violations, $713,341 exposure",
   "bound_in": [
    "process_mining/p2p_conformance.py",
    "Zagentexecution/sap_data_extraction/scripts/p2p_stdref_xray.py"
   ],
   "failure_mode": "applying a market normative model without checking the tenant's real shape. A standard O2C model here would report 100% non-conformance because there are ZERO deliveries — a false finding.",
   "improve": "exists for exactly ONE flow. The four public-sector flows have no normative model at all. THIS IS PRIORITY 3."
  },
  "B5_ocel2_object_centric": {
   "operates_on": "process events",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "one event references MULTIPLE object types, with no forced single-case notion",
   "why_it_matters": "ahead of most commercial tooling. Flat single-case mining forces an artificial case ID and distorts anything convergent or divergent — which public-sector finance is constantly: one budget, many commitments, many actuals.",
   "bound_in": [
    "Zagentexecution/sap_data_extraction/scripts/ocel_build_p2p.py"
   ],
   "failure_mode": "choosing object types badly reproduces the single-case distortion in a more expensive format",
   "improve": "this is the correct substrate for budget-to-actual, where 'the case' is genuinely ambiguous. Build it there next."
  },
  "C1_component_resolution_chain": {
   "operates_on": "repository",
   "origin": "OURS",
   "state": "STRONG",
   "does": "object -> TADIR (package) -> TDEVC (component id) -> DF14L (application component)",
   "why_it_matters": "the strongest algorithm in the inventory because it is DETERMINISTIC: SAP states the answer instead of us pattern-matching a name. 1.61M objects resolvable.",
   "bound_in": [
    "brain_v2/system_profile/probes/extract_component_hierarchy.py"
   ],
   "failure_mode": "a name-range chunk that returns exactly the cap truncates silently",
   "improve": "function modules resolve only via their function group; resolve them directly via TFDIR"
  },
  "C2_ascent_with_provenance_rung": {
   "operates_on": "repository",
   "origin": "OURS",
   "state": "STRONG",
   "does": "climb from any object to the installation, ALWAYS recording which rung of the evidence ladder resolved it",
   "why_it_matters": "the rung IS the point — a curated assignment must never be presentable as an inference (CP-003). 92.2%.",
   "bound_in": [
    "brain_v2/system_profile/build_model_graph.py"
   ],
   "failure_mode": "measuring the percentage against the wrong denominator. s097: 182 synthesised concepts and 24 users counted as 'unresolved' when they can never carry a component.",
   "improve": "add recency — an object with no execution in the window is a different verdict from one with none at all"
  },
  "C3_static_edge_extraction": {
   "operates_on": "repository",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "parse ABAP source for reads_tables / writes_tables / calls_fms and merge the edges into the graph",
   "bound_in": [
    "brain_v2/parse_abap_edges.py",
    "brain_v2 graph build"
   ],
   "why_weak": "only 98 of 1,212 objects carry code edges. Most of the graph is connected by crossings, not by parsed structure.",
   "failure_mode": "a WRONG edge is worse than a missing one — it sends someone to change the wrong thing. The parser is deliberately conservative: unambiguous SELECT/INSERT/CALL FUNCTION forms only, full-line comments stripped so a commented-out SELECT never becomes a dependency, dynamic forms skipped.",
   "improve": "the 134 code objects with NO source need ADT extraction — that is the real remaining gap",
   "denominator_correction_s097": "\"98 of 1,212 objects have edges\" measured the WRONG POPULATION — tables, GL accounts, variants and synthesised concepts cannot have code edges. Against CODE objects it was 86/245 (ABAP_REPORT 64%, ABAP_CLASS 78%). The remaining 159 split into a PARSER gap (25 with source on disk, closed in s097 — 8 yielded edges) and an EXTRACTION gap (134 with no source, needs ADT). Two different problems wearing one number. SECOND TIME this exact mistake appeared this session: the ascent had it too."
  },
  "D1_D3_delta_strategies": {
   "operates_on": "data",
   "origin": "MARKET_STANDARD",
   "state": "STRONG",
   "does": "PK-delta for master, value-compare for totals, high-water-mark for transactional",
   "why_it_matters": "the intelligence is the ROUTING (table class -> strategy), not the strategies themselves",
   "bound_in": [
    "scripts/extraction/gold_refresh.py",
    "Zagentexecution/sap_data_extraction/scripts/delta_refresh_2026.py"
   ],
   "failure_mode": "the sync log records domain, table and strategy but NEVER the system — with one golden DB per system that ambiguity is now structural",
   "improve": "add the system column to _gold_sync_log"
  },
  "D4_field_splitting": {
   "operates_on": "data",
   "origin": "OURS",
   "state": "STRONG",
   "does": "split a wide table read into column groups against the 512-byte line buffer, re-join locally",
   "bound_in": [
    "Zagentexecution/mcp-backend-server-python/rfc_helpers.py"
   ],
   "failure_mode": "a re-join on a non-unique key silently multiplies rows"
  },
  "D5_bounded_probe_with_cap_reporting": {
   "operates_on": "data",
   "origin": "OURS",
   "state": "STRONG",
   "does": "read with an explicit cap and date filter; report a hit cap as '>=cap', NEVER as a count",
   "why_it_matters": "the discipline IS the algorithm — a truncated read reported as a total is how a model starts lying",
   "bound_in": [
    "brain_v2/system_profile/probes/probe_footprint.py"
   ],
   "failure_mode": "the one it exists to prevent"
  },
  "E1_crossing": {
   "operates_on": "model",
   "origin": "OURS",
   "state": "STRONG",
   "does": "join profile x capability x claims x documents x companions through an explicit canonical key",
   "produces": "system-level blind spots — modules the tenant runs that the model never examined",
   "bound_in": [
    "brain_v2/system_profile/build_profile_links.py"
   ],
   "failure_mode": "comparing names raw instead of canonically. That defect appeared THREE times in one session.",
   "improve": "the claim matcher still has a text fallback; report it as weak linkage rather than merging it with structured evidence"
  },
  "E2_coherence": {
   "operates_on": "model",
   "origin": "OURS",
   "state": "STRONG",
   "does": "compare what the macro asserts top-down against what the detail evidences bottom-up",
   "why_it_matters": "the ONLY mechanism that detects the model drifting from the system it describes",
   "bound_in": [
    "brain_v2/system_profile/build_model_graph.py"
   ],
   "failure_mode": "a defect in the checker reads as a defect in the model — which is what happened, and is why it must fail loudly"
  },
  "E3_trigger_evaluation": {
   "operates_on": "model",
   "origin": "OURS",
   "state": "WORKS",
   "does": "compare state against thresholds across accumulation / maturity / interpretation families; return what to re-run and why",
   "bound_in": [
    "brain_v2/methods/check_triggers.py"
   ],
   "failure_mode": "thresholds chosen by judgement can be so loose nothing ever fires, which looks identical to a healthy model",
   "improve": "derive thresholds from observed variance"
  },
  "E4_canonicalisation": {
   "operates_on": "model",
   "origin": "OURS",
   "state": "WORKS",
   "does": "resolve any spelling to a canonical key through a declared alias contract, in exactly one place",
   "bound_in": [
    "brain_v2/canonical.py"
   ],
   "failure_mode": "the defect it removes: reporting something MISSING that exists under an alias"
  },
  "F1_interface_boundary_analysis": {
   "operates_on": "interfaces",
   "origin": "OURS",
   "state": "WORKS",
   "does": "enumerate the boundary (RFCDES / EDIDC / TBTCP / ICFSERVICE) and CORRELATE the configuration against observed traffic in the call stream. Configuration says what someone once set up; the call stream says what happens today; the GAP is the finding, in both directions.",
   "why_it_matters": "the boundary carries 1.33M executions and 37 documented flows, and the map is PROSE written by hand: it cannot detect a new destination appearing, cannot go stale visibly, and cannot be reproduced on a second installation (INSTANCE, not KIT)",
   "improve": "1. fix the destination regex and remove the row cap so DEAD is a fact, not a sample. 2. resolve the middleware GUIDs from the middleware side — SAP cannot name them. 3. bind each live destination to the PROCESS FLOWS it serves, not only to domains.",
   "bound_in": [
    "process_mining/interface_boundary.py"
   ],
   "first_run": "238 destinations configured -> 8 LIVE, 230 DEAD (configured, zero traffic: maintenance and attack surface nobody uses), 40 UNDECLARED (traffic with no RFCDES entry, including the MuleSoft logical-port GUIDs at 75,066 calls). 9 IDoc message types / 19,400 documents. 5,822 active HTTP services.",
   "failure_mode": "TWO defects declared on its own first run, because an algorithm that hides its own weaknesses is worse than none: (1) the destination parse TRUNCATES names ('HQ-SAP-P0', cut GUIDs), so DEAD/UNDECLARED counts carry parse error; (2) the call-stream scan is capped at 400,000 rows of 15.6M, so it is a SAMPLE — a destination could be marked DEAD purely because its traffic fell outside the sample."
  },
  "C1c_custom_fm_domain": {
   "operates_on": "repository",
   "origin": "OURS",
   "state": "WORKS",
   "does": "custom Z/Y function module -> canonical domain, via the curated tfdir_custom.APP_DOMAIN overlay",
   "why_it_matters": "custom FMs have no SAP component by definition, and they are precisely the objects the differentiation rests on. Y_BAPI_WBS_FINANCIAL_DATA_1 (974,868 calls) resolved to nothing until this rung existed — the single highest-volume business function module in the tenant was invisible to the model.",
   "bound_in": [
    "brain_v2/component_map.py"
   ],
   "failure_mode": "the overlay is CURATED (3,073 FMs). An uncurated custom FM resolves to nothing and looks like a technical object.",
   "improve": "derive the overlay from what each custom FM READS/WRITES rather than curating it by hand"
  },
  "F2_satellite_derivation": {
   "operates_on": "interfaces",
   "origin": "OURS",
   "state": "WORKS",
   "does": "group boundary endpoints into SATELLITES by call signature, resolve what each calls, and bind it to the domains and flows it drives",
   "why_it_matters": "each interface DEFINES a satellite; the satellite is the ORIGIN of the transaction; the origin is what associates it to a flow. In a system orchestrated 80% from outside, the satellite catalogue IS the process map at the boundary.",
   "bound_in": [
    "process_mining/derive_satellites.py"
   ],
   "first_run": "40 satellites from 7,783,106 calls; 12 GUID FLEETS RECOVERED. Independently rediscovered MuleSoft: a 21-endpoint fleet calling Y_BAPI_WBS_FINANCIAL_DATA_1 974,868 times -> PS -> PROJ2FUND, and a 17-endpoint fleet on Y_BAPI_YPS8 -> PS/PSM_FM -> B2C2A, P2P, POST2BUDGET, PROJ2FUND.",
   "failure_mode": "endpoint names TRUNCATE in the parse, so one fleet can split into two — the 17- and 21-endpoint fleets share a GUID prefix and may be the same satellite. Grouping by call signature mitigates this but does not fix the parse.",
   "improve": "fix the truncation, then merge fleets whose endpoint prefixes match"
  },
  "A7_concept_drift": {
   "operates_on": "logs",
   "origin": "MARKET_STANDARD",
   "state": "WORKS",
   "does": "monthly activity profile per domain as RATES PER DAY (executions, distinct objects, distinct users); a month is flagged when at least TWO signals depart from the trailing baseline by more than 50%. One signal moving is noise; two is a change of shape.",
   "why_it_matters": "replaces the threshold heuristics in check_triggers with a question put to the log itself: did the process change, and when? A biennium organisation has an EXPECTED drift at the boundary; any other drift is the finding worth having.",
   "bound_in": [
    "process_mining/detect_drift.py"
   ],
   "failure_mode": "TWO defects found on its own first runs, both worth recording because both produce confident nonsense: (1) comparing monthly VOLUMES across unequal months produced 11 signals, almost all in the first FULL month — the window is 2026-02-21..2026-06-21, so February has 3 days and April 30. Fixed by normalising per day. (2) z-scores over a two-month baseline exploded to z=1016 because the standard deviation collapses. Fixed by using relative change, which is robust at n=2 and readable. An absurd number dressed as a statistic is worse than a plain one.",
   "current_reading": "3 signals, ALL in 2026-06, all 'distinct objects up', across UNRELATED domains (CTS_Transport, HCM, Basis_Security). Same month + same signal + unrelated domains is the signature of a CAPTURE CHANGE, not process drift. Recorded as a SUSPICION, not a finding.",
   "improve": "needs about a year of history before a z-score is meaningful and before an expected biennium boundary can be told apart from an anomaly. It strengthens purely by the accumulator continuing to run — which is the argument for protecting A2."
  },
  "C4_object_role_derivation": {
   "operates_on": "repository",
   "origin": "OURS",
   "state": "WORKS",
   "does": "derive what each object is FOR — posting engine, report, interface, worklist, master-data maintenance, checker, batch engine, technical — from signals we already hold: edges, execution channel, audience size, volume and text",
   "why_it_matters": "the model could say where an object BELONGS and not what it DOES. 'PBC has 33 objects' is not knowledge; '2 posting engines, 14 reports, one operator on the worklist' is. A domain with readers and no writer is CONSUMED, not operated — and no list of names can say that.",
   "bound_in": [
    "process_mining/derive_object_roles.py"
   ],
   "first_run": "178 objects: 55 REPORT, 46 POSTING_ENGINE, 27 MASTER_DATA_MAINTENANCE, 23 INTERFACE, 12 WORKLIST, 9 CHECKER, 4 TECHNICAL, 2 BATCH_ENGINE. INDEPENDENT CONFIRMATION of the operating model: PS, Travel, BusinessPartner, Procurement_P2P and Treasury_EBS all flagged DRIVEN FROM OUTSIDE — derived from object ROLES, not from the audit log's origin axis. Two independent methods agreeing is real validation.",
   "failure_mode": "a role is a HYPOTHESIS. A report that writes a log table looks like a writer; a getter that posts looks like a reader. The confidence and the evidence are returned so a reader can disagree — a role asserted without its evidence would be worse than none.",
   "improve": "feed it the flows: an object's role WITHIN a flow (approver, poster, reconciler) is sharper than its role in general"
  }
 },
 "_portfolio": {
  "by_operand": {
   "logs": 7,
   "process_events": 5,
   "repository": 5,
   "data": 3,
   "model": 4,
   "interfaces": 2
  },
  "by_origin": {
   "MARKET_STANDARD": 8,
   "MARKET_EXTENDED": 1,
   "OURS": 12
  },
  "by_state": {
   "STRONG": 11,
   "WORKS": 12,
   "WEAK": 0,
   "FRAGILE": 1,
   "UNDER_EXERCISED": 0,
   "MISSING": 0
  },
  "_reading": "Twelve algorithms have no market equivalent — that is the defensible surface. Eight are adopted standards, correctly, because reinventing DFG discovery would be vanity. The single MISSING one covers the integration boundary, and the single FRAGILE one is the log accumulator, which is simultaneously the most valuable algorithm here and the least protected."
 },
 "_priorities": {
  "1": "A2 — protect the log accumulator. FRAGILE, unversioned, and the only algorithm whose value is destroyed by delay.",
  "2": "golden cases for the unguarded algorithms — 20 of 22 can regress silently today.",
  "3": "B4 — normative models for the public-sector flows; authored, not yet implemented.",
  "_note": "F1 was priority 1 and is now built. The mechanism (improve_algorithms.py) re-ranks these from measured signals rather than from memory."
 },
 "_doctrine_how_to_build_better_algorithms": {
  "_why_here": "the reasoning that produces the next algorithm must live with the algorithms, not in a conversation",
  "premise": "Algorithms are not one asset among others — they are the ONLY asset that survives the client. Data ages from the moment it is extracted. Findings age: '80.6% external' is true for Feb-Jun 2026. Tools are bindings — p2p_conformance.py is one algorithm tied to one flow, one schema, one tenant. The ALGORITHM is what travels to installation #2.",
  "move_1_change_the_substrate_not_the_algorithm": "OCEL 2.0 instead of a flat log: the SAME discovery algorithms, radically better fidelity on convergent processes. Public-sector finance is convergent constantly — one budget, many commitments, many actuals. A flat log forces an artificial case ID and distorts exactly what we need to see. Cheapest and highest-leverage of the three.",
  "move_2_add_the_axis_the_market_lacks": "A3 is this move already: process mining assigns activities but does not systematically resolve WHO CALLED. In a system orchestrated 80% from outside, the caller IS the process. Adding a dimension to a proven algorithm is cheaper and more defensible than inventing one.",
  "move_3_author_the_normative_content": "We do not compete by building a better conformance checker — the market has one and reinventing it is vanity. We compete by being the only ones who know what 'correct' means for budget→commitment→actual. The ALGORITHM is theirs; the public-sector NORMATIVE CONTENT is ours. That is where the moat is.",
  "move_4_declare_the_failure_mode": "Every algorithm states how it LIES, not only what it does. The market ships algorithms as black boxes. In a domain where being confidently wrong is expensive, publishing the failure mode is itself a differentiator — and it is the only thing that makes an algorithm reviewable."
 },
 "_new_asset_classes_s097": {
  "_note": "ranked by value. Classes 1-3 now exist; class 4 is a declared slot.",
  "1_normative_models": {
   "status": "CREATED",
   "where": "brain_v2/normative_models/normative_models.json",
   "why": "B4 conformance is the heart of the product and is worthless without something to measure against. One normative model existed before this (P2P 3-way match); the four public-sector flows had none."
  },
  "2_event_log_definitions": {
   "status": "DECLARED, extraction pending",
   "where": "currently buried as 'configs' inside sap_process_discovery.py",
   "why": "the table→event mapping (case_id, activity, timestamp, resource) per flow is reusable across clients and is invisible today"
  },
  "3_failure_modes": {
   "status": "CREATED as a field on every algorithm; queryable view pending",
   "why": "'in how many known ways can this model lie?' is the question an auditor asks and no vendor answers"
  },
  "4_benchmarks": {
   "status": "SLOT — cannot be filled before client #2",
   "why": "'in a UN agency, staff cost is X% of budget'. The slot must exist NOW: without it the second client gets analysed as if it were the first and the comparison is lost."
  }
 },
 "_nesting": {
  "_insight": "USER, s097: algorithms are NESTED. You start from the standard, then the logs, then discover interfaces and correlate — and each interface or connection DEFINES A SATELLITE, which gives you the ORIGIN of the transaction, which is what associates it to a flow and a domain.",
  "_why_it_was_wrong_before": "the catalogue was a FLAT LIST organised by operand. But an algorithm's output is another's input: this is a directed graph. Treating it as a list is why F1 (boundary) and A3 (origin axis) ran INDEPENDENTLY — F1 enumerated destinations, A3 parsed raw host strings — and a fleet of 174 middleware endpoints collapsed into one label because A3 had no satellite catalogue, only strings.",
  "layers": {
   "L0_INVARIANT": {
    "algorithms": [
     "C1_component_resolution_chain"
    ],
    "depends_on": "nothing but SAP itself",
    "note": "the ground truth every other answer is anchored to. Improving it lifts everything above."
   },
   "L0b_CUSTOM_OVERLAY": {
    "algorithms": [
     "C1c_custom_fm_domain"
    ],
    "depends_on": "the curated APP_DOMAIN overlay",
    "note": "custom Z/Y function modules have NO SAP component by definition — their function group lives in a customer package DF14L cannot resolve. They need their own rung, and they are exactly the objects the product thesis rests on because no commercial tool can label them either."
   },
   "L1_OBSERVATION": {
    "algorithms": [
     "A1",
     "A2",
     "A4",
     "A6"
    ],
    "depends_on": [
     "L0"
    ],
    "note": "what runs, when, by whom — classification anchored on the standard"
   },
   "L2_BOUNDARY": {
    "algorithms": [
     "F1_interface_boundary_analysis"
    ],
    "depends_on": [
     "L1 (traffic to correlate configuration against)"
    ],
    "produces": "the satellite catalogue"
   },
   "L3_ORIGIN": {
    "algorithms": [
     "F2_satellite_derivation",
     "A3_two_axis_classification"
    ],
    "depends_on": [
     "L2 (the catalogue)",
     "L0/L0b (to resolve what each satellite calls)"
    ],
    "note": "a satellite is an ENTITY, not a caller string: identity, channel, volume, direction, the domains it touches"
   },
   "L4_FLOW": {
    "algorithms": [
     "B1",
     "B2",
     "B3",
     "B5"
    ],
    "depends_on": [
     "L3 (origin) + the flow model"
    ],
    "note": "process mining runs per FLOW, never per module"
   },
   "L5_CONFORMANCE": {
    "algorithms": [
     "B4_declarative_conformance"
    ],
    "depends_on": [
     "L4 (a flow)",
     "a NORMATIVE MODEL"
    ],
    "note": "worthless without the normative model — which is why that asset class exists"
   },
   "L6_SELF_CHECK": {
    "algorithms": [
     "E1",
     "E2",
     "E3"
    ],
    "depends_on": [
     "everything below"
    ]
   }
  },
  "_consequences": {
   "1": "an algorithm CANNOT be better than its dependencies. A3's origin resolution is capped by F1's quality — today F1 truncates destination names, so A3 inherits that error.",
   "2": "improving a LOW layer lifts everything above it. The L0 fix (DF14L instead of regex) took the frontier from 40% to 7.7% without touching any algorithm above it.",
   "3": "the improvement mechanism must weight by DEPTH, not only by state. improve_algorithms.py does not yet — that is a declared gap."
  }
 },
 "_improvement_axes": {
  "_note": "improve_algorithms.py ranks by HEALTH — unguarded, stale, fragile. That is maintenance. These four axes rank by DISTANCE TO THE GOAL, which is a product that instantiates on another organisation and explores its domains autonomously.",
  "1_from_classification_to_investigation": "our algorithms answer 'what is this?' but none generates its own next question. mine_domain.py exists and is barely used; the registry is still hand-curated (128 objects vs 286 auto-resolved). A5 is the only algorithm that learns.",
  "2_from_observation_to_modelling_a_flow": "we can see execution; we cannot yet build an event log for a flow nobody hand-configured. To model B2C2A we would write it by hand — that does not scale to 8 flows x N clients. The event-log DEFINITIONS must become an asset.",
  "3_from_n1_to_generalisable": "EVERY algorithm is validated on ONE tenant. 43 golden cases, all UNESCO facts. COMPONENT_TO_DOMAIN is a hand-written map reflecting UNESCO's module set. MEASURED: only 22.7% of resolved executions are tenant-invariant; 2,617,419 rest on 334 hand-curated entries.",
  "4_from_confident_to_calibrated": "DONE s097 — resolve_domain() now returns (domain, confidence, rung, evidence, tenant_invariant). Before this, an answer SAP asserts and an answer we curated were indistinguishable to every consumer. An algorithm that cannot say HOW it knows cannot be triaged, audited, or ported — and cannot be improved deliberately, because you cannot tell a lucky answer from a sound one.",
  "_the_measure": "brain_v2/methods/measure_portability.py — the low-confidence answers ARE the map of what breaks next."
 }
}